#include <iostream>
using namespace std;
int main() {
    int age;
    cout<<"enter your age:";
    cin>>age;
    if(age>=18){
        cout<<"elligible for voting";
    }
    else
    {
        cout<<"not elligible";
    }
   

    return 0;
}
